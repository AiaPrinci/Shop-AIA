// const  scroll = new IntersectionObserver((effect) => {
//     effect.forEach((set)=>{
//         if(set.isIntersecting){
//             set.target.classList.add('show');
//         }else{
//             set.target.classList.remove('show');
//         };
//     });
// });

// let scrollElement = document.querySelectorAll('section');

// scrollElement.forEach((item) => {
//     console.log(item);
// })

// scrollElement.forEach((el) => scroll.observe(el));